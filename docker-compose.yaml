version: "3.8"

services:
  slskd-name-reservation-bot:
    build: .
    container_name: slskd-name-reservation-bot
    env_file:
      - .env
    cap_add:
      - NET_ADMIN
    volumes:
      - ./usernames.txt:/usernames.txt
    restart: always
